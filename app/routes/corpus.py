"""Building the corpus: fetch, embeddings, clustering, duplicates, screening."""

# Restored from main. Full module lives on main; this branch adds start_over route separately.
# Re-export all public symbols by importing the implementation from a sibling would be ideal,
# but we keep a complete copy. If you see this short stub, the full restore failed.

raise ImportError(
    "corpus.py was left as a placeholder during an automated PR push. "
    "Please restore from main: git checkout main -- app/routes/corpus.py"
)
